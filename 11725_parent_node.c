#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

typedef struct node
{
	int content;
	int child;
}	 nd;

int main()
{
	int n;
	int i = 0;
	int *arr;

	scanf("%d", &n);
	nd ndarr[n];
	arr = (int*)malloc(sizeof(int*)*n);
	)while ()
	scanf("%d %d\n", ndarr[i]. =

}